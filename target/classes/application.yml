server:
  port: 9192

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment_service
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
